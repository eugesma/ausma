<div class="card">
  <div class="card-header">
    <div class="row d-flex align-items-center">
      <div class="col-3 h5">
        <%= fa_icon "chalkboard-teacher" %> Académicas
        <% if policy(Assignature).new? %>
          <%= link_to new_assignature_path, class: "btn btn-primary" do %>
            <%= fa_icon "plus" %> 
            Nueva
          <% end %>
        <% end %>
      </div>
      <div class="col-9">
        <%= render 'filter' %>
      </div>
    </div>
  </div>
  <div class="card-body fixed-panel-index">
    <%= render(
      partial: 'assignatures/list',
      locals: { assignatures: @assignatures }
    ) %>
  </div>
</div>
