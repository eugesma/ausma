<div class="col-md-8 mx-auto">
<div class="card">
  <div class="card-header bg-warning">
    <h4><%= fa_icon "edit" %> Editando configuración</h4>
  </div>
  <div class="card-body fixed-panel">
    <%= render 'form', config: @config %>
  </div>
  <div class="card-footer text-right">
    <%= link_to config_path(Config.first), class: "btn btn-light" do %>
      Volver
    <% end %>
    <button type='submit' name='commit' class='btn btn-success' form='edit_config_<%= @config.id %>'>
      <%= fa_icon "save" %> Guardar cambios
    </button>
  </div>
</div>
</div>