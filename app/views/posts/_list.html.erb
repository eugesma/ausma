<div id="filterrific_results">
  <% if @posts.present? %>
    <div class="card fixed-panel-list">
      <table class="table table-hover table-striped table-sm">
        <thead>
          <tr>
            <th>Nombre</th>
            <th>Tipo</th>
            <th>Docentes</th>
            <th>Créditos</th>
            <th></th>
          </tr>
        </thead>
        <tbody id="users">
          <%= render @posts %>
        </tbody>
      </table>
    </div>
    <div id="page_links">
      <%= will_paginate @posts, list_classes: %w(pagination justify-content-center), renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
    </div>
  <% else %>
    <%= render(
      partial: 'shared/not_found_filter',
      locals: { object_name: 'publicaciones' }
    ) %>
  <% end %>
</div>