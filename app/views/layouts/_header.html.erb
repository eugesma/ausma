<nav class="navbar sticky-top navbar-expand-sm  bg-dark navbar-dark">
  <a class="navbar-brand" id="lig-brand" href="/">
    <%= image_tag("uncoma-1.png", size: "55x55", alt: "Logo Uncoma", class: 'logo') %>
    AUSMA SEDasd
  </a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsibleNavbar">
    <span class="navbar-toggler-icon"></span>
  </button>
  <!-- Links -->
  <div class="collapse navbar-collapse justify-content-end" id="collapsibleNavbar">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <% if policy(Assignature).index? %>
          <li class="nav-item <%= active_class('assignatures') %>">
            <%= link_to assignatures_path, class: 'nav-link' do %>
              <%= fa_icon "chalkboard-teacher" %> Asignaturas
            <% end %>
          </li>
        <% end %>
        <% if policy(Dedication).index? %>
          <li class="nav-item <%= active_class('dedications') %>">
            <%= link_to dedications_path, class: 'nav-link' do %>
              <%= fa_icon "users" %> Reuniones
            <% end %>
          </li>
        <% end %>
        <% if policy(ExtensionActivity).index? %>
          <li class="nav-item <%= active_class('extension_activities') %>">
            <%= link_to extension_activities_path, class: 'nav-link' do %>
              <%= fa_icon "hiking" %> Actividades
            <% end %>
          </li>
        <% end %>
        <% if policy(User).index? %>
          <li class="nav-item <%= active_class('users') %>">
            <%= link_to users_admin_index_path, class: 'nav-link' do %>
              <%= fa_icon "user-tie" %> Docentes
            <% end %>
          </li>
        <% end %>
      <% end %>
    </ul>
    <ul class="nav navbar-nav">
      <%= render 'layouts/user_menu' %>
    </ul>
  </div>
</nav>