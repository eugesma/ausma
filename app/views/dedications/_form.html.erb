<%= form_with(model: dedication, local: true) do |form| %>
  <% if dedication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dedication.errors.count, "error") %> prohibited this dedication from being saved:</h2>

      <ul>
      <% dedication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :hours %>
    <%= form.text_field :hours %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
