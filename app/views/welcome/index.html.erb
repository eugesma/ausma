  <!-- Header -->
  <header class="masthead">
    <% if user_signed_in? %>
      <% if current_user.has_any_role?(:admin, :secretaria) %>
        <%= render "secretary_dashboard" %>
      <% end %>
      <% if current_user.has_any_role?(:docente)%>
        <%= render "teacher_dashboard"%>
      <% end %>
    <% else %>
      <%= render "devise/sessions/new_session" %>
    <% end %>
  </header>