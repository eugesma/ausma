<div class="row">
  <div class="col-md-4">
    <div class="list-group">
      <%= link_to assignatures_path, class: "list-group-item list-group-item-action justify-content-between align-items-center" do %>
        <%= fa_icon "chalkboard-teacher" %> Asignaturas
        <span class="badge badge-primary badge-pill pull-right"><%= Assignature.count %></span>
      <% end %>
      <%= link_to meetings_path, class: "list-group-item list-group-item-action justify-content-between align-items-center" do %>
        <%= fa_icon "users" %> Reuniones
        <span class="badge badge-primary badge-pill pull-right"><%= Meeting.count %></span>
      <% end %>
      <%= link_to extension_activities_path, class: "list-group-item list-group-item-action justify-content-between align-items-center" do %>
        <%= fa_icon "hiking" %> Actividades de extensión
        <span class="badge badge-primary badge-pill pull-right"><%= ExtensionActivity.count %></span>
      <% end %>
      <%= link_to users_admin_index_path, class: "list-group-item list-group-item-action justify-content-between align-items-center" do %>
        <%= fa_icon "user-tie" %>
        Docentes
        <span class="badge badge-primary badge-pill pull-right"><%= User.count %></span>
      <% end %>
    </div>
  </div>
</div>