<div class="card">
  <div class="card-header">
    <div class="row">
      <div class="col-lg-8 col-md-8">
        <h3 class="title">
          <%= fa_icon "clipboard-check" %> Asistencias
          <%= link_to new_month_presence_path, class: "btn btn-primary" do %>
            <%= fa_icon "plus" %>
            Nuevo formulario
          <% end %>
        </h3>
      </div>
      <div class="col-md-1"><%= render_filterrific_spinner %></div>
      <div class="col-md-3">
        <%= form_for_filterrific @filterrific do |f| %>
          <div class="input-group">
            <%= f.text_field(
              :search_by_name,
              class: 'filterrific-periodically-observed form-control py-2 border-right-0 border',
              placeholder: 'Nombre',
              autocomplete: 'off',
              ) 
            %> 
            <span class="input-group-append">
              <div class="input-group-text bg-transparent"><i class="fa fa-search"></i></div>
            </span>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="card-body">
    <% if @month_presences.present? %>
      <%= render(
        partial: 'month_presences/list',
        locals: { month_presences: @month_presences }
      ) %>
    <% else %>
      <%= render(
        partial: 'shared/not_found',
        locals: { object_name: 'formularios' }
      ) %>
    <% end %>
  </div>
</div>

