<div class="card">
  <div class="card-header">
    <div class="row">
      <div class="col-8">
        <h3 class="title">
          <%= fa_icon "hiking" %> Actividades de extensión
          <%= link_to new_extension_activity_path, class: "btn btn-outline-primary" do %>
            <%= fa_icon "plus" %> 
            Nueva
          <% end %>
        </h3>
      </div>
      <div class="col-1"><%= render_filterrific_spinner %></div>
      <div class="col-3">
        <%= form_for_filterrific @filterrific do |f| %>
          <div class="input-group">
            <%= f.text_field(
              :search_by_name,
              class: 'filterrific-periodically-observed form-control py-2 border-right-0 border',
              placeholder: 'Nombre',
              autocomplete: 'off',
              ) 
            %> 
            <span class="input-group-append">
              <div class="input-group-text bg-transparent"><i class="fa fa-search"></i></div>
            </span>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="card-body">
    <% if @extension_activities.present? %>
      <%= render(
        partial: 'extension_activities/list',
        locals: { extension_activities: @extension_activities }
      ) %>
    <% else %>
      <%= render(
        partial: 'shared/not_found',
        locals: { object_name: 'actividades de extensión' }
      ) %>
    <% end %>
  </div>
</div>
