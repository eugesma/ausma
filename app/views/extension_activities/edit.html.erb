<ul class="breadcrumb">
  <li class="breadcrumb-item"><%= link_to 'Inicio', root_path %></li>
  <li class="breadcrumb-item"><%= link_to 'Actividades de extensión', extension_activities_path %></li>
  <li class="breadcrumb-item">Editando actividad</li>
</ul>

<div class="col-md-8 mx-auto">
<div class="card">
  <div class="card-header bg-warning">
    <h4><%= fa_icon "edit" %> Editando actividad</h4>
  </div>
  <div class="card-body">
    <%= render 'form', extension_activity: @extension_activity %>
  </div>
  <div class="card-footer text-right">
    <%= link_to extension_activities_path, class: "btn btn-light" do %>
      Volver
    <% end %>
    <%= link_to @extension_activity, class: "btn btn-secondary" do %>
      <%= fa_icon "eye" %> Ver
    <% end %>
    <button type='submit' name='commit' class='btn btn-success' form='edit_extension_activity_<%= @extension_activity.id %>'>
      <%= fa_icon "save" %> Guardar cambios
    </button>
  </div>
</div>
</div>